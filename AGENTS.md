# AGENTS.md

## 今日头条热点发帖规则

## 发布入口

- 通过浏览器发帖时固定使用入口：`https://mp.toutiao.com/profile_v4/graphic/publish?from=toutiao_pc`。
- 自动发帖属于对第三方平台发布内容；执行最终发布前需要用户明确确认。
- 用户确认前，只做选题、素材、正文、图片和本地保存，不点击最终发布。

## 目录规则

- 本任务所有素材和结果都保存在 `tasks/toutiao_hot_posts/` 下。
- 候选和草稿按日期分层，并且一个选题一个文件夹。
- 日期目录使用北京时间日期，格式为 `YYYY-MM-DD`。
- 候选选题默认保存到 `candidates/YYYY-MM-DD/<HHMM_选题名>/`。
- 每个选题文件夹必须包含该选题自己的标题、正文、来源、评分、图片和状态信息（详见"选题文件夹规则"）。
- 采集到的通用原始材料可保存到 `materials/YYYY-MM-DD/`。
- 已发送、淘汰、过期的选题分别归档到 `archive/sent/YYYY-MM-DD/`、`archive/rejected/YYYY-MM-DD/`、`archive/expired/YYYY-MM-DD/`。
- 全局发送索引和题材指纹保存到 `state/`，用于防止重复发送。
- 爆款选择算法的版本、权重、发帖后表现和复盘记录保存到 `state/`，至少包括 `selection_model.json`、`post_performance.jsonl`、`experiment_log.md`。
- 不使用 Telegram 采集任务的规则、脚本、输出目录或状态文件。

## 选题文件夹规则

- 每个选题文件夹必须包含以下文件，候选阶段就要全部配齐，不允许把任何一项推迟到"用户确认后再写"：
  - `titles.txt`：3-5 条备选标题。
  - `body.txt`：完整正文初稿（用户复核阶段做的是改稿，不是从零写）。
  - `sources.md`：来源链接、平台、抓取时间、排名、热度或互动数据；末尾保留"待补抓项"清单。
  - `score.json`：优先级、本批排名、及时性分、`emotion_priority`、同质化风险、稀缺角度、评论欲评分、普通人代入感、冲突强度、反常识程度、标题点击力、账号匹配度、风险收益比、追更价值、首图点击力、爆款概率、评分版本、当前权重、实验分组、预测依据、推荐动作。
  - `status.json`：选题状态、创建/更新时间、是否已发送、发送时间、关联旧题材、`emotion_priority`、`new_progress_marker`、`next_action`。
  - `images/`：该选题对应图片，至少 2 张可复核图片；含 `image_candidates.md`（每张图匹配理由）与 `image_notes.md`（来源、版权、裁剪决策）。
- 状态枚举（`status.json.status`）：`candidate_pending_review` / `approved` / `sent` / `rejected` / `expired` / `progress_update`。
- `emotion_priority` 字段固定结构：`{ trigger, intensity, reason }`，三项必填。
- 文件夹名格式 `<HHMM_选题名>`，HHMM 固定使用本批候选的**抓取时刻**（北京时间），不是发帖时间；选题名要简短，避免特殊符号。
- 同一热点如果第二天出现官方通报、当事人回应、处罚结果、数据更新、反转等新进展，可以在新日期下新建选题文件夹，但必须在 `sources.md` 和 `status.json` 中标注关联旧题材。
- 没有新进展的跨天旧热点不得新建同类选题。

## 已发送归档和防重复规则

- 选题最终发布成功后，必须把完整选题文件夹移动或复制到 `archive/sent/YYYY-MM-DD/`。
- 每次发送成功后，必须追加记录到 `state/sent_index.jsonl`。
- `state/sent_index.jsonl` 至少记录：发送时间、标题、核心关键词、来源链接、内容指纹、题材指纹、归档路径。
- 发帖后必须把表现数据追加到 `state/post_performance.jsonl`，并关联 `sent_index.jsonl` 中的题材指纹和内容指纹。
- 防重复不能只看标题，必须同时检查标题相似度、核心关键词、来源链接、内容指纹和题材指纹。
- 同一新闻换标题、换表达、换配图后仍视为同一题材，默认不得重复发送。
- 只有出现明确新进展时，才允许在 `status.json` 中标记为“新进展续写”，并再次进入候选。

## 及时性规则

- 每次准备发帖前重新抓取热点，不复用上一轮旧候选。
- 每条候选必须记录抓取时间、平台、标题、链接、排名、热度或可见互动数据。
- 默认按北京时间判断日期；候选必须是当天热点，或旧事件出现当天新进展。
- 优先选择首次发现时间近、排名上升快、热度增长快的题材。
- 同一题材若在 30-60 分钟内跨平台扩散，优先级上调。
- 旧事件只有出现官方通报、当事人回应、处罚结果、数据更新、反转等新钩子时才可再次进入候选。
- 已进入尾声的复盘型话题降权，尤其是只剩“怎么看”“为什么又火了”“后续来了”但无新事实的题材。
- 每天 3 次发帖时间固定为北京时间 `14:00`、`17:00`、`20:00`。
- 三个发帖时段不默认发布同一类题材：`14:00` 优先快讯、新进展、官方通报；`17:00` 优先民生冲突、消费维权、职场出行、教育医疗等下班前讨论型题材；`20:00` 优先强情绪、家庭、教育、食品安全、公共安全、普通人遭遇类题材。
- 每次发帖前都重新计算候选，不提前锁死全天素材。

## 抓取规则

- 默认源：百度热搜、微博热搜、B站热门，必要时叠加今日头条本站热榜、知乎热搜。每批快照保存到 `materials/YYYY-MM-DD/<source>.json`，文件名带抓取时刻。
- 候选生成前必须先比对 `state/sent_index.jsonl` 与 `state/topic_fingerprints.jsonl`，命中（标题相似度 / 关键词 / 来源链接 / 内容指纹 / 题材指纹任一吻合）的直接淘汰，不进 `review_queue.md`。
- 直连失败 fallback 顺序（按优先级降级，每降一级要在 `review_queue.md` 顶部写明）：
  1. 用第二个热榜接口或镜像（tophub、rebang 等）补抓。
  2. 用搜索引擎多关键词组合（事件主体+日期+处置词）反查具体事件链接，找到原始报道再确认时效。
  3. 用浏览器工具（Chrome 扩展）打开新闻源页面，按"图片匹配规则"截图归档；同时把页面文字保存到 `sources.md`。
  4. 找用户人工提供热榜快照或截图。
- 任何 fallback 路径不得用来跳过"图片必须先从新闻源本身找"的硬性要求；若新闻源原图最终拉不到，必须在 `image_candidates.md` 写明缺口，等用户决定是否继续找或换题。
- 抓取过程对图片、原始报道、关键页面元素的获取失败次数与原因记入 `materials/YYYY-MM-DD/抓取日志.md`，便于复盘。

## 情绪优先级规则

- 容易强烈触发愤怒、心疼、流泪、恐惧、荒诞感或公共安全焦虑的新闻，默认列为最高优先级候选。
- 最高优先级题材包括但不限于：
  - 官方通报类：食品安全、校园安全、医疗、养老、殡葬、交通、消防、执法、平台责任等有明确处置进展的事件。
  - 挑动愤怒类：伤害消费者、欺负弱者、突破底线、恶意欺骗、明显不公、监管失守、平台失责。
  - 挑动共情类：普通人遭遇困境、老人儿童弱势群体、救助与善意、家庭悲剧、重大损失、离别和遗憾。
  - 反差冲突类：表面体面但背后失序、低价便利背后代价、规则漏洞、职业伦理崩塌、公众信任被破坏。
- 此类题材只要同时满足“今天新进展 + 事实可核验 + 有公共讨论价值”，优先级可直接上调到 `A` 或 `A+`。
- 旧事件如果当天出现官方通报、处罚结果、涉事方回应、监控/录音/文件曝光、调查反转，按新进展重新评估情绪优先级。
- 情绪强不等于无底线煽动：标题和正文可以有态度，但不得编造细节、扩大伤亡、攻击个人隐私、消费受害者痛苦或制造未经证实的阴谋。
- 这类题材的稀缺角度不能只停留在“骂人”和“震惊”，必须写出可讨论的增量：监管盲区、平台责任、普通人防范、制度漏洞、处置是否到位。
- 每条候选在 `status.json` 与 `score.json` 中都要写 `emotion_priority`，结构固定为 `{ trigger, intensity, reason }`，三项必填，不允许只写其一。

## 稀缺性规则

- 写作前先查重，检查今日头条、百度、微博等平台已有标题和观点。
- 如果已有内容大量使用同类标题、同类叙事或同类情绪表达，该题材同质化风险上调。
- 不直接复述“发生了什么”，优先写“为什么现在发生”“背后机制”“普通人受到什么影响”“反常识点”。
- 内容必须有增量：时间线、多平台观点对比、官方或当事人最新回应、行业背景、数据变化、可验证细节至少占一种。
- 标题要避开热搜原词堆叠，避免与平台已有爆文标题高度相似。
- 最终选题不是只看热度，而是选择“热度正在上升 + 已有内容没写透 + 我们能写出新角度”的题材。

## 爆款概率评分规则

- 目标不是“保证爆款”，而是稳定提高爆款概率，并减少低效选题；只看热搜排名不合格，必须同时评估“热点 × 切口 × 标题 × 评论欲 × 发布时间 × 账号人设”。
- 每条候选必须在 `score.json` 写入 `viral_probability`，并按以下公式综合判断：`爆款概率 = 热度上升速度 × 情绪强度 × 普通人代入感 × 评论争议度 × 角度稀缺性 × 标题点击力 × 账号匹配度 - 合规风险`。
- `comment_desire_score`：判断用户看完是否有话想说，是否容易站队、吐槽、补充亲身经历或争论责任边界；如果预判不出 3 个高概率评论方向，默认降权。
- `ordinary_person_relevance_score`：判断题材是否贴近普通人的孩子、老人、打工、消费、房子、医疗、教育、出行、食品安全、钱袋子等现实利益。
- `conflict_score`：判断是否存在清晰冲突结构，例如消费者 vs 商家、家长 vs 学校、普通人 vs 规则、弱者 vs 强势方、平台 vs 用户、官方处置 vs 公众期待。
- `counterintuitive_score`：判断是否具备“怎么会这样”的反常识点，例如看似小事但代价巨大、看似合法但不合理、看似个案却有普遍性。
- `title_click_score`：每个候选都要单独评估标题是否有信息缺口、情绪钩子、具体对象和反差点；标题不能只是热搜词重排。
- `account_fit_score`：判断题材是否匹配账号历史更容易起量的内容类型；不因为全网热度高就机械入选。
- `risk_reward_score`：同时评估流量收益和账号风险；涉未成年人、伤亡、性别对立、地域对立、医疗纠纷、执法争议等题材必须单独标注风险等级。
- `follow_up_value_score`：判断后续是否可能出现官方通报、处罚、回应、反转、调查进展；有明确追更空间的题材优先级上调。
- `cover_click_power_score`：首图不只要合规和匹配，还要判断能否让用户停下来；优先选择信息密度高、事实指向明确、视觉冲突强的证据图或现场图。
- 候选入池最低要求：至少同时具备“今天新进展或热度上升”“普通人代入感明确”“可预判评论区争议”“有可写增量”四项；缺任一项时，除非有极强独家信息或官方通报，否则不得列为 A 级以上。

## 实验进化规则

- 爆款评分不是固定公式，而是可验证假设；每次选题必须记录当时使用的 `scoring_version`、`feature_weights`、各项分值、预测爆点和放弃理由，方便事后验证。
- `state/selection_model.json` 保存当前评分版本、字段权重、最近一次调整时间、调整原因和禁用规则；没有数据支撑时，不允许凭感觉大改权重。
- 每篇发布后至少在 `30分钟`、`2小时`、`24小时` 三个节点记录表现：展现、阅读、点击率、评论、点赞、收藏、转发、粉丝增长、负反馈、审核/限流异常、评论区主要争议点；平台没有提供的指标写明“不可见”。
- `state/post_performance.jsonl` 每条记录必须关联：发布时间段、标题、题材类型、情绪触发点、普通人代入点、冲突结构、首图类型、评分版本、预测爆款概率、实际表现。
- 每天发完最后一篇后做轻量复盘：比较“预测高但表现低”“预测低但表现高”“被淘汰但后来爆了”的题材，写入 `state/experiment_log.md`。
- 每周或累计满 10 篇后做一次权重复盘：连续带来高表现的特征上调权重，连续误判的特征下调权重；每次调整必须保留旧版本，不能覆盖历史判断。
- 候选池必须保留少量探索位：默认每批至少 1 条不是最高分但有明显新角度、强评论欲或新题材潜力的候选，用来发现新的爆款模式。
- 不能为了自进化牺牲底线：防重复、事实核验、图片匹配、合规风险、用户最终确认发布这几条规则永远优先于算法推荐。
- 自进化只自动调整“推荐优先级和评分权重”，不自动点击发布；最终发布仍必须经过用户明确确认。

## 图片匹配规则

- 图片必须先从新闻源本身找，不允许先去站内推荐、热点列表或无关新闻里随便拿图。
- 图片优先级固定为：
  1. 新闻源正文里的事件现场图、官方通报图、公告图、文件图。
  2. 新闻源页面干净截图，优先截标题、发布时间、正文关键段、官方来源、关键事实段。
  3. 官方机构、当事平台、监管部门页面截图。
  4. 其他媒体同一事件的原图或页面截图。
  5. 合规图库的强相关场景图。
  6. 自制信息图，只能做占位或补充，不能作为默认首选图。
- 直接截取新闻源页面时，只截正文/标题/关键事实区域；必须避开导航栏、右侧推荐、广告、浮窗、无关图片、站点热点图和明显水印。
- 如果原图自带媒体水印、平台角标或版权标识，不做去水印处理；能通过裁剪自然避开且不影响事实表达时才保留，否则跳过该图。
- 每张候选图都必须在 `images/image_candidates.md` 写清楚“匹配理由”，格式为：图片对应新闻里的哪一句事实、哪个人物/地点/政策/物品/场景。
- 只写“站内相关图”“民生类配图”“财经类配图”不合格；无法说清楚对应哪条新闻事实的图片必须剔除。
- 同一选题至少保留 2 张可复核图片：一张“新闻源证据图”，一张“事件/政策/现场强相关图”。找不到第二张时必须注明缺口，等待用户确认是否继续找。
- 不同选题的图片不能统一套同一个信息图模板；同一选题也不能用与正文无关的装饰图充数。
- 首图必须额外评估 `cover_click_power_score`：是否信息密度高、事实指向明确、视觉上有停留价值；只有“相关但平淡”的图片不能作为默认首图。
- 使用媒体图片前必须保存来源链接，并在用户复核后再决定是否用于发布。

## 快速选图流程（固定步骤，每次照做，不超 5 分钟）

**目标**：候选确认后，快速拿到 2-3 张真实新闻图片。

**流程**（严格按照顺序做，不要跳过）：

1. **打开新闻源页面**
   从 `sources.md` 里取已确认的原始新闻链接，在浏览器打开。

2. **只看正文中的图片，不搜图库，不看推荐区**
   - ✅ 正文里的现场图、当事人截图、官方通报截图、公告图、文件图。
   - ✅ 如果正文没有配图：截取页面标题+正文前两段区域作为干净的页面截图。
   - ⛔ **禁止**从右侧"相关推荐""猜你喜欢""热点推荐"区域下载任何图片。
   - ⛔ **禁止**去图库搜关键词，禁止用装饰图充数。

3. **下载图片**
   - 右键 → 另存为 → 存到 `images/` 目录
   - 命名规则：`source_01_匹配内容简述.jpg/png`
   - 只保留最核心的 2-3 张，不要全下载
   - 如果正文配图不够，最多再截一张页面干净截图（只截标题+正文区域，不截广告/推荐/导航）

4. **写说明文件（极简版）**
   `image_candidates.md` 只用写：
   ```
   ## 可用图片
   1. source_01_XX.jpg → 匹配哪句事实 → 建议首图/备选
   2. source_02_XX.jpg → 匹配哪句事实 → 备选
   ```
   `image_notes.md` 只用写：来源URL、尺寸、格式。
   不需要多余的分析和评语。

5. **不纠结**
   - 如果新闻源正文一张图都没有：写上"新闻源无原图"，换下一候选。
   - 如果下载的图有水印/角标：不去水印，直接保留，用户复核决定。
   - 如果图片太小（<200px）：放弃，换下一候选。
   - 全程不超过 5 分钟。图片有就行，不用完美。

## 候选输出格式

- 每次先给用户候选，不直接发帖。
- 候选阶段必须把"选题文件夹规则"里列的所有文件写齐（titles / body / sources / score / status / images 全部），用户复核做的是审稿和取舍，不是从零补素材。任何一项缺失都视为候选未完成。
- 当批所有候选汇总到 `candidates/YYYY-MM-DD/review_queue.md`，至少标注：热度来源、及时性判断、情绪触发点、同质化风险、稀缺角度、评论欲、普通人代入感、冲突结构、反常识点、标题点击力、账号匹配度、风险收益比、追更价值、首图点击力、爆款概率、推荐优先级；明细字段写到对应选题的 `score.json`。
- `review_queue.md` 顶部必须包含"抓取与防重复说明"段落，写明本批抓取走了哪条路径（直连/搜索/浏览器/用户提供）、覆盖范围是否打折、与 `state/` 历史比对结果。
- 用户确认后进入定稿+配图复核+发布；未确认前不点最终发布按钮。

## 文案润色风格

- 默认使用今日头条观点文风格：信息密度高、口语自然、有明确判断，但不过度煽情。
- 开头 2-3 句必须给出事实钩子和观点钩子，不使用“近日”“引发热议”这类空泛开头作为主句。
- 正文优先采用“事实简述 -> 关键矛盾 -> 普通人视角 -> 结论判断”的结构。
- 语言要像真人作者，不使用 AI 常见套话，如“值得注意的是”“不难发现”“从某种意义上说”“总的来说”“这背后反映出”。
- 少用排比、宏大总结和万能金句，多用具体细节、短句、转折和判断。
- 不伪造信息，不写无法核验的内幕、数据或因果。
